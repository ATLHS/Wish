const config = {};

module.exports = config;

process.env = Object.assign(process.env, {
  SMTP_USER_EMAIL: "leroileonidas@hotmail.com",
  SMTP_USER_PASSWORD: "sparta",
  SMTP_HOST: "smtp-mail.sparta.com",
});
